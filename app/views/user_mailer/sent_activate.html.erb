<%= @header %>

下記URLにアクセスして、ユーザ登録をお願いします。
なお、本メール到着後<%= h User.activation_lifetime %>日が経過すると、下記URLは無効となりますのでご注意ください。

<%= @signup_url %>

<% if enable_activate? -%>
無効になってしまった場合、以下のURLにアクセスし、[<%= _('Register your profile and start using SKIP') %>]リンクをクリックして下さい。
ユーザ登録をやり直すことが出来ます。

<%= @site_url %>

<% else -%>
無効になってしまった場合は本メール末尾の連絡先へお問い合わせ下さい。

<% end -%>
<%= @footer %>
