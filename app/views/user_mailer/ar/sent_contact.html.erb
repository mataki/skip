<% if @owner.is_a?(Group) -%>
    <%= @owner.name %>
<% end -%>
    <%= @entry.title %>
<% if Admin::Setting.contact_mail_contain_contents -%>

<%= convert_plain(@entry) %>


<%= _("Click the URL below to see all.")%>

    <%= url_for(:controller => :board_entries, :action => :forward, :id => @entry.id, :only_path => false) %>
<% else -%>

<%= _("Click the URL below to see the content of message.")%>

    <%= url_for(:controller => :board_entries, :action => :forward, :id => @entry.id, :only_path => false) %>
    <%= _("*Jumps directly to the blog entry.") %>
<% end -%>

<%= @footer %>
