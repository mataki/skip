<%# locals: feeds -%>
<% feeds.each do |feed| %>
  <div class="right_box ui-corner-all">
    <h2 class="ui-corner-top"><%= h feed.channel.title -%></h2>
    <ul class="no_style">
      <% feed.items.each do |item| %>
        <% title = (Time.now.ago(5.day) < item.pubDate) ? "#{icon_tag('new')} #{h(item.title)}" : h(item.title) -%>
        <li><%= link_to title, h(item.link), :target => "_blank" -%></li>
      <% end %>
    </ul>
  </div>
<% end %>
