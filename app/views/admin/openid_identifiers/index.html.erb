<%= render :partial => '/admin/data_menu_box' %>
<% content_for :topic_path do -%>
<%= render :partial => '/admin/shared/topic_path' %>
<% end -%>
<% content_for :contents_right do -%>


<div class="box tabular">
  <%= render :partial => '/admin/shared/sortable_table',
             :locals => {
               :columns => %w[url],
               :objects => @openid_identifiers,
               :class_name => 'Admin::OpenidIdentifier',
               :link_elem_size => 2,
               :link_elem_proc => lambda { |object|
      result = ""
      result << content_tag(:td, link_to(_('Edit'), edit_admin_user_openid_identifier_path(@user, object)) )
      result << content_tag(:td, link_to(_('Destroy'), admin_user_openid_identifier_url(@user, object), {:confirm => _('Are you sure?'), :method => :delete}) )
    }
             }
  -%>
</div>
<%= link_to _('New %{model}') % {:model => _('openid identifier')}, new_admin_user_openid_identifier_path if @openid_identifiers.empty? %>
<% end -%>
