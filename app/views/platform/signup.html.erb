<div class="content">
  <h3><%= _('Signup') %></h3>
  <p><%= "以下で指定されたメールアドレスにユーザ登録のためのURLを送信します。" %></p>
  <% form_tag :action => :signup do -%>
    <table>
      <tr>
        <th><label for="email">メールアドレス</label></th>
        <td><%= text_field_tag(:email, params[:email]) %></td>
      </tr>
    </table>
    <%= submit_tag "送信する" %>
  <% end -%>
</div>
