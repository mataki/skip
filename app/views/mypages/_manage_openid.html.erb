<% content_for :contents_left do -%>
  <%= render :partial => "manage_menu", :locals => { :selected_menu => @menu } %>
<% end -%>

<% content_for :contents_right do -%>
  <div class="desc ui-corner-all margin05emver"><p><%= _('Enter your OpenID URL in the form below.')%></p></div>

  <%= error_messages_for 'openid_identifier' %>

  <div class="edit_form_space ui-corner-all">
    <% form_for :openid_identifier, @openid_identifier, {:url => {:action => :apply_ident_url}} do |f| -%>
    <table class="normal user_info">
      <tr>
        <th width="100px"><%= f.label :url, _("OpenID URL") %></th>
        <td style="padding:5px;"><%= f.text_field :url, :name => "openid_url" %></td>
      </tr>
    </table>
    <%= submit_tag _('Save') %>
    <% end -%>
  </div>
<% end -%>
