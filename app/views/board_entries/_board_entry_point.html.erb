<%
 # ポイントボタンのパーツ
 # :locals => { :entry, :editable }
-%>

<% if editable %>
  （<%= "#{entry.point} #{Admin::Setting.point_button}" %> 頂きました）
<% else %>
  <input type="button" id="entry_point_button" value="<%= "#{entry.point} #{Admin::Setting.point_button}" -%>" />
<% end %>
</span>
