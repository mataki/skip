<%
 # ポイントボタンのパーツ
 # :locals => { :entry }
-%>

<% if entry.point_incrementable?(@current_user) -%>
  <input type="button" id="entry_point_button" value="<%= "#{entry.point} #{h Admin::Setting.point_button}" %>" />
<% else -%>
  （<%= "#{entry.point} #{h Admin::Setting.point_button}" %> 頂きました）
<% end -%>
