<%
 # ポイントボタンのパーツ
 # :locals => { :entry }
-%>

<% if entry.point_incrementable?(@current_user) -%>
  <% form_tag url_for(:controller => :board_entries, :action => :ado_pointup, :id => entry.id), :id => 'entry_point_form' do -%>
    <%= submit_tag "#{entry.point} #{h Admin::Setting.point_button}" %>
  <% end -%>
<% else -%>
  (<%= _("(Got %{point} %{point_button} from you)") % {:point => entry.point , :point_button => h(Admin::Setting.point_button)}%>)
<% end -%>
